services:
  - type: web
    name: mirna-salon-erp
    runtime: node
    plan: free
    buildCommand: "true"
    startCommand: node server.js
    healthCheckPath: /api/settings
    envVars:
      - key: NODE_ENV
        value: production
